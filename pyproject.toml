[project]
name = "imagec"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "Pillow>=12.3.0,<13",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.17.0",
    "pytest>=8.4.1",
]
