[project]
name = "imagec"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "py7zr>=0.22.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.17.0",
    "pytest>=8.4.1",
]
