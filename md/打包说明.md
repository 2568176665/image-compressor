1.pyinstaller
pip install pyinstaller
pyinstaller -F -w --upx-dir upx --optimize 2 image_compressor.py

参数说明
参数	作用
-F	打包成单个 exe 文件
-w	GUI 程序模式，隐藏黑框窗口
--upx-dir upx	使用 UPX 压缩工具（需已安装并加入 PATH）
--optimize 2	优化级别为 2（最高级别）
image_compressor.py	你的主脚本文件名