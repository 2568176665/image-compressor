# 编码器与发布说明

项目已移除 ImageMagick runtime，发布包使用 `src/third_party/codecs/windows-x64/` 中的固定版本编码器。

编码器资源由 `manifest.json` 管理。修改或替换任意 EXE、DLL 或许可证文件后，需要重新生成对应 SHA-256；否则启动检查和 `build.py` 都会拒绝继续。

许可证与来源请查看仓库根目录的 [THIRD_PARTY_NOTICES.md](../THIRD_PARTY_NOTICES.md)。
