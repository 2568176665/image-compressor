1.pyinstaller
pip install pyinstaller
pyinstaller -F -w --upx-dir upx --optimize 2 image_compressor.py

基本参数
scriptname 必填参数，要打包的 Python 脚本，比如：

bash
pyinstaller --onefile main.py
-h / --help 显示帮助信息。

-v / --version 显示 PyInstaller 版本。

🧩 打包模式
-D / --onedir 默认模式，生成一个文件夹，里面有 exe 和依赖文件。

-F / --onefile 打包成单个 exe 文件（常用）。

🧩 输出配置
--specpath DIR 指定 .spec 文件保存路径（打包配置文件）。

-n NAME / --name NAME 指定生成的 exe 名称。

--distpath DIR 指定打包输出目录（默认是 dist/）。

--workpath DIR 指定临时构建目录（默认是 build/）。

🧩 资源与依赖
--add-data SOURCE:DEST 添加额外数据文件（如图片、配置），格式：源路径:目标路径。

--add-binary SOURCE:DEST 添加额外二进制文件。

-p DIR 添加额外的 Python 搜索路径。

--hidden-import MODULENAME 指定隐藏导入的模块（PyInstaller有时漏掉）。

🧩 优化与调试
-d {all,imports,bootloader,noarchive} 调试模式，输出更多信息。

--optimize LEVEL 优化字节码（0=无，1=基本，2=更强）。

--clean 清理临时文件后再打包。

--log-level LEVEL 设置日志等级（DEBUG/INFO/WARN/ERROR）。

🧩 界面相关
-c / --console 默认，显示控制台窗口。

-w / --windowed GUI 程序模式，不显示控制台窗口。

--hide-console {hide-late,hide-early,minimize-late,minimize-early} 控制台隐藏或最小化的方式。

-i FILE.ico 指定 exe 图标。

--splash IMAGE_FILE 打包时添加启动画面。

🧩 高级功能
--noupx / --upx-dir UPX_DIR 控制是否使用 UPX 压缩，或指定 UPX 路径。

--exclude-module EXCLUDES 排除某些模块。

--uac-admin / --uac-uiaccess 设置 exe 需要管理员权限。

--version-file FILE 添加版本信息资源。

--manifest FILE 添加 manifest 文件。

2.Nuitka
Windows → 建议安装 Visual Studio Build Tools 或 MinGW/Clang
Linux/macOS → 使用系统自带的 gcc/clang

pip install nuitka
pip install ordered-set zstandard
nuitka --onefile image_compressor.py
nuitka --onefile --lto --clang --windows-disable-console image_compressor.py
